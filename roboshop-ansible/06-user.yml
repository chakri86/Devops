- name:  configuring user server
  hosts: user
  become: yes
  tasks: 

  - name: disable nodejs default module
    command: dnf module disable nodejs -y

  - name: enable nodejs 20.x module
    command: dnf module enable nodejs:20 -y

  - name: install nodejs in server
    package:
      name: nodejs
      state: present

  - name: create system user for user
    user:
      name: roboshop
      system: yes
      home: /app
      shell: /sbin/nologin
      state: present
      comment: "Roboshop system user"

  - name: remove app directory if already exists
    file:
      path: /app
      state: absent

  - name: delete user.zip in in tmp directiry
    file: 
      path: /tmp/user.zip
      state: absent

  - name: create app directory
    file: 
      path: /app
      state: directory

  - name: download users
    get_url:
      url: https://roboshop-artifacts.s3.amazonaws.com/user-v3.zip
      dest: /tmp/user.zip

  - name: unzipping the user
    unarchive: 
      src: /tmp/user.zip    
      dest:  /app
      remote_src: yes

  - name: install npm
    community.general.npm:
      path: /app

  - name: copy user service
    template: 
      src: user.service.j2
      dest: /etc/systemd/system/user.service

  - name: deamon reload
    systemd_service:
      name: user
      daemon_reload: true
      
  - name: enable and start user
    service:
      name: user
      state: restarted
      enabled: yes
